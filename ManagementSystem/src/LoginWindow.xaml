<Window x:Class="Employee_Management_System.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    
        Title="Login" Height="600" Width="920" WindowStartupLocation="CenterScreen"
        Background="#3F5B75" Foreground="White" WindowStyle="SingleBorderWindow" ResizeMode="CanResize" AllowsTransparency="False">
    <!-- <Window.Resources> -->


        <!-- Custom Button Style -->
        <!-- <Style TargetType="Button">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                CornerRadius="5">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources> -->
   
    <!-- <Grid>

        <Border Background="Black" CornerRadius="10" Padding="20" Margin="40">

            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Width="300" Background="White"> 

                <Border BorderBrush="#1ABC9C" BorderThickness="2" CornerRadius="5" Margin="0,10" Background="Red">
                    <TextBox x:Name="UsernameTextBox" Width="280" Height="40" Text="Enter Username" FontSize="18" Foreground="Gray" 
                             GotFocus="TextBox_GotFocus" LostFocus="TextBox_LostFocus" 
                             Background="Transparent" BorderThickness="0" Padding="5"/>
                </Border>
                
                <Border Background="Red" BorderBrush="#1ABC9C" BorderThickness="2" CornerRadius="5" Margin="0,10">
                    <PasswordBox x:Name="PasswordBox" Width="280" Height="40" FontSize="18"
                                 Background="Transparent" BorderThickness="0" Padding="5"/>
                </Border>
                
                <Button Content="Login" Width="200" Height="50" Margin="0,20,0,0"
                        Click="btnLogin_Click" Background="#1ABC9C" FontSize="18"
                        Foreground="White" FontWeight="Bold" BorderThickness="0">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border Background="{TemplateBinding Background}" 
                                    CornerRadius="6" Padding="5">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                        </ControlTemplate>
                    </Button.Template>
                </Button>

            </StackPanel>
        </Border>

            <Border Background="Red" BorderBrush="#1ABC9C" BorderThickness="2" CornerRadius="5" Margin="0,10">
          
            <Image Source="Assets/login.png" Width="200" Height="200" Margin="10,10,10,10"/>
        </Border>
    </Grid> -->


    <Grid>
    <!-- Define Columns -->
    <Grid.ColumnDefinitions>
        <ColumnDefinition Width="350" />
        <ColumnDefinition Width="550" />
    </Grid.ColumnDefinitions>

    <!-- Logo Image -->
    <Image Source="C:\Users\DELL\OneDrive\Desktop\Employee_Management_System\ManagementSystem\Assets\login.png" Width="200" Height="400" HorizontalAlignment="Center" VerticalAlignment="Center" Grid.Column="0"/>

    <!-- Login Panel -->
    <Border Background="Black" CornerRadius="10" Padding="20" Margin="10,10,10,10" Grid.Column="1">

        <!-- This is for all Login Panel -->
        <StackPanel Width="450" Height="400" Background="White" Orientation="Vertical" VerticalAlignment="Center" HorizontalAlignment="Center"> 

            <!-- This is for Username and Password -->
            <!-- <StackPanel Width="350" Height="320" Background="Orange" Orientation="Vertical">  -->
            <Border Margin="0,10">
                <TextBox x:Name="UsernameTextBox" Width="300" Height="50" Text="Enter UserID" FontSize="22" Foreground="White"               GotFocus="TextBox_GotFocus" LostFocus="TextBox_LostFocus" 
                    Background="#34495E" BorderThickness="2" Padding="5"
                    TextAlignment="Center"
                     Margin="0,70,0,0"/>
            </Border>

            <Border Margin="0,10">
                <TextBox x:Name="PasswordTextBox" Width="300" Height="50" Text="Enter Password" FontSize="22" Foreground="White" Background="#34495E" 
                GotFocus="PasswordTextBox_GotFocus" LostFocus="PasswordTextBox_LostFocus" 
                BorderThickness="1" Padding="5" TextAlignment="Center"/>
            </Border>
            <!-- </StackPanel> -->

            <!-- This is for Login and SignUp button -->
            <Border Margin="0,10">
            <StackPanel Width="350" Height="180" Orientation="Horizontal" VerticalAlignment="Center" HorizontalAlignment="Center" Background="White"> 
                    <!-- Login -->
                <Button Content="Login" Width="150" Height="50" Margin="10,10,10,10"
                        Click="btnLogin_Click" Background="#34495E" FontSize="18"
                        Foreground="White" FontWeight="Bold" BorderThickness="0">
                        <!-- <Button.Template>
                            <ControlTemplate TargetType="Button">
                            <Border Background="{TemplateBinding Background}" 
                                CornerRadius="6" Padding="5">
                              <ContentPresenter HorizontalAlignment="Center"/>
                            </Border>
                            </ControlTemplate>
                        </Button.Template> -->
                    </Button>
                    <!-- SignUp -->
                    <Button Content="SignUp" Width="150" Height="50" Margin="10,10,10,10"
                        Click="btnLogin_Click" Background="#1ABC9C" FontSize="18"
                        Foreground="White" FontWeight="Bold" BorderThickness="0">
                        <!-- <Button.Template>
                            <ControlTemplate TargetType="Button">
                            <Border Background="{TemplateBinding Background}" 
                                CornerRadius="6" Padding="5">
                              <ContentPresenter HorizontalAlignment="Center"/>
                            </Border>
                            </ControlTemplate>
                        </Button.Template> -->
                    </Button>

                </StackPanel>
            </Border>

        </StackPanel>
    </Border>


<!-- 

<Border Background="Black" CornerRadius="10" Padding="20" Margin="40" Grid.Column="1">
        <StackPanel Width="300" Height="300" Background="White" Orientation="Horizontal"> 

            <Button Content="Login" Width="200" Height="50" Margin="0,20,0,0"
                    Click="btnLogin_Click" Background="#1ABC9C" FontSize="18"
                    Foreground="White" FontWeight="Bold" BorderThickness="0">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="6" Padding="5">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Button.Template>
            </Button>

            <Button Content="SignUp" Width="200" Height="50" Margin="0,20,0,0"
                    Click="btnLogin_Click" Background="#1ABC9C" FontSize="18"
                    Foreground="White" FontWeight="Bold" BorderThickness="0">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="6" Padding="5">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Button.Template>
            </Button>

        </StackPanel>
    </Border> -->
</Grid>



</Window>

